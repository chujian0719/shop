export const globalError = state => {
    return state.global.error;
};

export const globalLoading = state => {
    return state.global.loading;
};

export const globalTitle = state => {
    return state.global.title;
};

export const globalHeaderType = state => {
    return state.global.headerType;
};

export const addressGetSelected = state => {
    return state.address.selected;
};
